# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: specs/auth/login.spec.ts >> AUTH — Login and Logout >> TC-AUTH — Authenticated user can log out
- Location: tests/specs/auth/login.spec.ts:107:7

# Error details

```
Error: expect(locator).toBeVisible() failed

Locator: locator('img[alt="User Avatar"]').locator('..').first()
Expected: visible
Timeout: 60000ms
Error: element(s) not found

Call log:
  - Expect "toBeVisible" with timeout 60000ms
  - waiting for locator('img[alt="User Avatar"]').locator('..').first()
    - waiting for" https://qe-automation.cialfo.sg/app/auth" navigation to finish...
    - navigated to "https://qe-automation.cialfo.sg/app/auth"

```

```yaml
- link "Cialfo logo Sign up for clearing updates Study at a Top 20 UK university 7500 Scholarships available Guaranteed accommodations Sign up for clearing updates":
  - img "Cialfo logo"
  - paragraph: Sign up for clearing updates
  - paragraph: Study at a Top 20 UK university
  - paragraph: 7500 Scholarships available
  - paragraph: Guaranteed accommodations
  - img "Sign up for clearing updates"
- text: QE Automation
- heading "Sign In" [level=1]: Good morning
- paragraph: Sign in to continue — students and counselors, all in one place.
- text: QS QE Student 3tzl6tj5n Not you? Password
- textbox "Password"
- checkbox "Remember me" [checked]
- text: Remember me Forgot Password? Sign In or continue with
- img "Google logo"
- text: Google
- img "Microsoft logo"
- text: Microsoft  Phone Number Students, if you do not have an account sign up here. Student Signup
```

# Test source

```ts
  1  | import { expect, Locator, Page } from '@playwright/test';
  2  | import { TermsPrivacyModal } from '../home/components/terms-privacy.modal';
  3  | import { ConnectionIntroduceModal } from '../home/components/connection-introduce.modal';
  4  | import { AvailabilityReminderModal } from '../home/components/availability-reminder.modal';
  5  | 
  6  | // Nav component — no URL or goto(); attaches to every authenticated page as persistent chrome.
  7  | // The user avatar menu, present on every logged-in page.
  8  | export class UserMenu {
  9  |   private readonly avatar: Locator;
  10 |   private readonly logoutItem: Locator;
  11 |   private readonly termsPrivacy: TermsPrivacyModal;
  12 |   private readonly connectionIntroduce: ConnectionIntroduceModal;
  13 |   private readonly availabilityReminder: AvailabilityReminderModal;
  14 | 
  15 |   constructor(private readonly page: Page) {
  16 |     this.avatar = page.locator('img[alt="User Avatar"]').locator('xpath=..').first();
  17 |     this.logoutItem = page.getByText('Log Out');
  18 |     this.termsPrivacy = new TermsPrivacyModal(page);
  19 |     this.connectionIntroduce = new ConnectionIntroduceModal(page);
  20 |     this.availabilityReminder = new AvailabilityReminderModal(page);
  21 |   }
  22 | 
  23 |   // Clicks the avatar, dismissing any stacked first-login pop-ups first, then logs out and waits for the sign-in redirect.
  24 |   async logout() {
> 25 |     await expect(this.avatar).toBeVisible();
     |                               ^ Error: expect(locator).toBeVisible() failed
  26 |     // First-login pop-ups stack over the avatar — dismiss top-down before clicking it.
  27 |     await this.availabilityReminder.dismissIfPresent();
  28 |     await this.connectionIntroduce.dismissIfPresent();
  29 |     await this.termsPrivacy.dismissIfPresent();
  30 |     await this.avatar.click();
  31 |     await this.logoutItem.click();
  32 |     await this.page.waitForURL(/\/auth\/signin/);
  33 |   }
  34 | }
```